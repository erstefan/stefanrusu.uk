{
  "name": "stefanrusu.uk",
  "version": "1.0.0",
  "description": "Personal website of Stefan Rusu",
  "author": "Stefan Rusu",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
